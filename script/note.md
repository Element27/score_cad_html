MVC

Model
View
Controller

model the structure of the data 
view rnders the data
controller is what ties the model to the view and handles the logic


model (we want to give the frontend a list of student)

view (we are sending the processed list of student from the backend to the frontend)

controller (we write the logic to organise, arrange etc the student data from the model adn send it to the view)


